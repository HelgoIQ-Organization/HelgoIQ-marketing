# Static files in /public (including Meta legal *.html) are served before this rule.
# functions/_middleware.ts returns those *.html paths as 200 (Pages would otherwise 308).
# SPA fallback for client routes. /api/* is served by Pages Functions.
/*    /index.html   200
